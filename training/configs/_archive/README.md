This directory contains archived configurations that are no longer used by the main application. 
The `data.yaml` here corresponds to an old `plant-gpcmi` experiment with duplicate typo labels ('healty ', 'sick '). It does not align with the current per-species architecture.
